{
  "name": "test-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "tsc --noEmit && esbuild index.ts --bundle > main.js",
    "dev": "esbuild index.ts --bundle --outdir=dist --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "*",
    "maplibre-contour": "file:..",
    "maplibre-gl": "5.6.2",
    "typescript": "*"
  }
}
